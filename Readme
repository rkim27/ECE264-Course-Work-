Coursework from advanced C programming course taken Fall 2019

These programs use a script called "Makefile" to call "main", which calls these functions. "main" and "Makefile" are written by instructors of the course. 
